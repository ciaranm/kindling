% kindling has this one natively, so leave it alone rather than decomposing it
% into a pile of disequalities that kindling cannot do at all.
predicate fzn_all_different_int(array[int] of var int: x);
